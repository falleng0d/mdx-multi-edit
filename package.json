{
  "name": "mdx-multi-edit",
  "version": "0.1.0",
  "description": "MDX Multi Edit is a web application that allows users to edit multiple MDX files at once\nas if they were a single file.",
  "author": "Mateus Junior",
  "license": "MIT",
  "homepage": "https://github.com/falleng0d/mdx-multi-edit",
  "workspaces": [
    "./server",
    "./client",
    "./shared"
  ],
  "scripts": {
    "dev:client": "cd client && bun run dev",
    "dev:server": "cd server && bun run dev --port 3000 --directory ./test/sample",
    "dev:shared": "cd shared && bun run dev",
    "dev": "concurrently --kill-others -p \"[{name}]\" -n \"SERVER,CLIENT,SHARED\" -c \"auto\" \"npm run dev:server\" \"npm run dev:client\" \"npm run dev:shared\"",
    "build:client": "cd client && bun run build",
    "build:shared": "cd shared && bun run build",
    "build": "bun run build:shared && bun run build:client",
    "postinstall": "bun run build:shared"
  },
  "keywords": [
    "bun",
    "hono",
    "react",
    "vite",
    "monorepo"
  ],
  "devDependencies": {
    "bun-types": "latest",
    "concurrently": "^9.1.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
